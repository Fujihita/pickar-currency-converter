{
  "name": "currency-converter",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node app.js",
    "test": "nyc mocha --exit",
    "report-coverage": "nyc report --reporter=text-lcov > coverage.lcov && codecov"
  },
  "dependencies": {
    "express": "~4.18.2",
    "mongodb": "^3.5.9"
  },
  "devDependencies": {
    "codecov": "^3.7.0",
    "mocha": "^8.0.1",
    "nyc": "^15.1.0",
    "should": "^13.2.3",
    "supertest": "^4.0.2"
  }
}
