{
  "name": "currency-converter",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node app.js",
    "test": "npx nyc mocha --exit --report lcovonly -- -R spec && codecov"
  },
  "dependencies": {
    "express": "~4.16.1"
  },
  "devDependencies": {
    "codecov": "^3.7.0",
    "mocha": "^8.0.1",
    "nyc": "^15.1.0",
    "should": "^13.2.3",
    "supertest": "^4.0.2"
  }
}
